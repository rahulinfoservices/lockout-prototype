import AlertDetails, {
  AlertDetailsProps,
} from "@/shared/components/domain/alert-details/alert-details";

export default function DeviceHealthDetails(props: AlertDetailsProps) {
  return <AlertDetails {...props} />;
}
